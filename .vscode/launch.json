{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Correr archivo JS actual",
      "type": "node",
      "request": "launch",
      "program": "${file}",
      "console": "integratedTerminal",
      "envFile": "${workspaceFolder}/.env"
    }
  ]
}